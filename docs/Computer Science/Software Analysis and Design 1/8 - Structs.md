- Structs are like classes, but they only have public properties
```cpp
struct StructName
{
   type propName1;
   type propName2;
};
```
- Initialization: `StructName instName = {valueForProp1, valueForProp2};`
- Access members through dot operator